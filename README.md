# Personal Accounting (WebView Android)

This is a tiny Android app that wraps `app/src/main/assets/index.html` inside a WebView.

## Build (GitHub Actions)
Push to GitHub and use Actions workflow: `.github/workflows/build-apk.yml`
The artifact `apks` will contain `app-debug.apk`.
